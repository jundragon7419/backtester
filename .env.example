# API 키는 keyring 사용을 우선한다:
#   python -c "import keyring, getpass; keyring.set_password('krx-backtester', 'KRX_API_KEY', getpass.getpass())"
# keyring에 없을 때만 아래 환경변수를 읽는다. 실제 키를 이 파일에 쓰지 않는다.
KRX_API_KEY=
